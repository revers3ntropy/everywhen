#!/bin/bash

svelte-kit sync

eslint --fix --config .eslintrc.cjs './src/**/*.{js,ts,svelte}' || exit 1

npx svelte-check \
  --tsconfig ./tsconfig.json \
  --compiler-warnings "css-unused-selector:ignore,unused-export-let:ignore" \
  "$@" || exit 1